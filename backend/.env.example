# 로컬 개발 환경 설정
# 이 파일을 .env 로 복사해서 사용하세요: cp .env.example .env

# PostgreSQL 데이터베이스 연결 URL (필수)
DATABASE_URL=postgresql://user:password@localhost:5432/credit_abandoner

# 서버 포트 (기본값: 8080)
PORT=8080

# 환경 (development / production)
NODE_ENV=development

# 세션 시크릿
SESSION_SECRET=your-secret-key-here
