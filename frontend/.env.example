# 로컬 개발 환경 설정
# 이 파일을 .env 로 복사해서 사용하세요: cp .env.example .env

# 백엔드 API 서버 URL (로컬 개발 시 설정 필요)
# 배포 환경(Replit 등)에서는 설정 불필요 - 같은 origin의 /api 자동 사용
VITE_API_BASE_URL=http://localhost:8080/api

# Vite 포트 (기본값: 5173)
# PORT=5173

# Vite base path (기본값: /)
# BASE_PATH=/
